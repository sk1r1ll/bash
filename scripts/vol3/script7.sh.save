#!/bin/bash

while [ -n "$1" ]
do
case "$1" in
-a) echo "-a - start calculator" ;;
toteal=$[ $1 + $2 ]
echo "The first parapeter is $1."
echo "The second parameter is $2."
echo "The sum is $total."
-b) echo "Not a calculator"
-c) echi "I didn't imagine else"
esac
shift
done
